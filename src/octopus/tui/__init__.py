"""Octopus-Agent Textual TUI — terminal-native interface for headless/SSH.

Provides a full terminal UI using Textual as an alternative to the
Rich+prompt_toolkit CLI. Shares the same kernel, agent loop, tools,
and providers as the CLI and GUI modes.
"""
